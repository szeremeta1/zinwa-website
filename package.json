{
  "name": "zinwa-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "live-server --open=./index.html --port=3000 --watch=.",
    "start": "live-server --open=./index.html --port=3000 --watch=."
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "dependencies": {
    "@vercel/analytics": "^1.5.0"
  }
}
